extends layout

block content
  h1= title
  p Welcome to #{title}
  
  ul 
    each val in messages
      li #{val.user}: #{val.text} | #{val.added}

  a(href='http://localhost:3000/new') 
    input(type='button', value='send message')